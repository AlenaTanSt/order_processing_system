## «апуск с параметрами по умолчанию:
```
.\build\solution\ops_app.exe
```

## «апуск с заданным количеством заказов:
```
.\build\solution\ops_app.exe 10000
```

## «апуск с нормальным завершением:
```
.\build\solution\ops_app.exe 10000 shutdown
```

## «апуск с принудительным завершением:
```
.\build\solution\ops_app.exe 10000 shutdown_now
```

## ѕримечани€

* ¬ режиме shutdown CLI провер€ет базовые инварианты (без прерывани€ выполнени€).
* ¬ режиме shutdown_now допустимо, что часть заказов не будет доставлена.
* ѕри малых значени€х q_*_capacity и коротких push_timeout возможно по€вление неуспешных submit() из-за backpressure Ч это ожидаемое поведение.